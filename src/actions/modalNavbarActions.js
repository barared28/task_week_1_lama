export const modalLoginShow = () => {
  return { type: "SHOWING_MODAL_LOGIN" };
};

export const modalLoginClose = () => {
  return { type: "CLOSE_MODAL_LOGIN" };
};

export const modalRegisterShow = () => {
  return { type: "SHOWING_MODAL_REGISTER" };
};

export const modalRegisterClose = () => {
  return { type: "CLOSE_MODAL_REGISTER" };
};
